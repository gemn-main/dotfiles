в main.c
```c
double per(double w, double h);

int main()
{
    printf("per = %.2f", per(12.4, 13.9));
    return 0;
}  
```

в func.c
```c
double per(double w, double h)
{
    if(w<0 || h<0)
        return 0;
    return 2 * (h + w);
}
```

к main подключается файл func через settings.json
```bash
"c": "cd $dir && gcc -std=c99 $fileName func.c -o $fileNameWithoutExt && $dir$fileNameWithoutExt",
```







/////////////////////////////////////////////////////////////////////////////////////////////////////




через сторонний файл funcc.h
```c
#ifndef __FUNCC_H__
#define __FUNCC_H__

double per(double, double);

#endif
```

в main 
```c
#include <stdio.h>
#include "funcc.h"

int main()
{
    printf("per = %.2f", per(12.4, 13.9));
    return 0;
}  
```

