```c
/*first program*/
#include <stdio.h>
int main(void)
{
    signed char byte_memory;
    byte_memory = 109;
    unsigned short a = 21;
    return 0;
}
```

нужны при объявлении переменной типом char 
![[Pasted image 20241022203614.png]]
чтобы указать будет ли знак в диапазоне или нет 


для всех остальных [[Типы данных|типов данных]]  автоматический(по умолчанию) применяется знаковый метод (signed) но также можно указать и unsigned если надо 


unsigned and signed НЕЛЬЗЯ писать с вещественными [[Типы данных|типами данных]] 
