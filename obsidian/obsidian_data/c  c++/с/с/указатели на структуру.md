```c
struct tag_vector
{
    double x, y;
};

int main(void)
{
    struct tag_vector vec = {1.4, 5.2};
    struct tag_vector *ptr_vec;

    ptr_vec = &vec;
    (*ptr_vec).x = 0.0001;
    double k = (*ptr_vec).y;
    printf("x = %f, y = %f", (*ptr_vec).x, vec.y);
    return 0;
}
...
x = 0.000100, y = 5.200000
```


для удобства можно исключить скобки у указателя 
```c
struct tag_vector
{
    double x, y;
};

int main(void)
{
    struct tag_vector vec = {1.4, 5.2};
    struct tag_vector *ptr_vec;

    ptr_vec = &vec;
    ptr_vec->x = 0.0001;
    double u = ptr_vec->y;
    printf("x = %f, y = %f", ptr_vec->x, vec.y);
    return 0;
}

...
x = 0.000100, y = 5.200000
```

[[указатели на структуру с выделением памяти]] 