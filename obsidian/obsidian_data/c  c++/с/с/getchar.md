```c
/*first program*/
#include <stdio.h>
int main(void){
        int value = getchar();
        return 0;
}
```
Функция `getchar` в языке C используется для чтения одного символа из стандартного ввода (обычно клавиатуры). Она объявлена в заголовочном файле `<stdio.h>`
работает похожим образом как и input в python



```c
#include <stdio.h>
int main() {
    int value1 = getchar();
    int value2 = getchar();
    printf("%c %c\n", value1, value2);
    return 0;
}
cd
c d
```