```c
#include <stdio.h>
int main() {
    double a, b;

    int res = scanf("%lf %lf", &a, &b);
    if(res != 2) {
        printf("Error");
        return 0;
    }

    float opana = a * b;
    printf("res = %f", opana);
    return 0;
}
```
