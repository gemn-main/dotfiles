```c
int main(){
    char str[10];
    scanf("%9s", str);
    puts(str);
    return 0;
}  
```
`9` - ограничивает ввод символов до 9 так как еще надо добавить символ конца строки `\0` 
но если встретит символ пробела завершиться ввод - эту проблему решает `fgets` 