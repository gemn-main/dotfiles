```cpp
struct point
{
private:
    int x, y;

public:
    point(int x, int y) // конструктор
    {
        this->x = x;
        this->y = y;
    }
    ~point()
    {
        cout << "вызов деструктора" << endl;
    }
    int get_x() { return x; }
    int get_y() { return y; }
};

int main()
{
    point pt(2, 2);
    cout << pt.get_x() << ' ' << pt.get_y() << endl;
    point pttt(1, 5);
}
...
2 2
вызов деструктора
вызов деструктора
```
выполняет код когда структура завершает свою работу
можно использовать как освобождение для памяти 