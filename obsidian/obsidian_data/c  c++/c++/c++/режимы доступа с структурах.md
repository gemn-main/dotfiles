```cpp
struct point {
private:
    int x, y;

public:
    double length() { return sqrt(x * x + y * y); }
};

int main()
{
    point pt;
    cout << pt.length() << endl;
    return 0;
}
```

оп умолчанию `public` 
`prinvate` - не дает доступ из вне но можно обратиться внутри структуры 