```cpp
double read_pow[5]{0};
ifstream ifs("out_course.dat", ios::in | ios::binary);
if (ifs.is_open())
{
	ifs.read((char *)read_pow, sizeof(pow));
}
for (double i : read_pow)
{
	cout << i << " ";
}
5.5 -51.1 0.51 -17 0 
```

`(char *)read_pow` - указатель на место куду записывать
`sizeof(pow)` - количество 