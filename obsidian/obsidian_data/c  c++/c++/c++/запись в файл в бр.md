```cpp
double pow[]{5.5, -51.1, 0.51, -17};
ofstream ofs("out_course.dat", ios::out | ios::binary);
if (ofs.is_open())
{
	ofs.write((char *)pow, sizeof(pow));
}
ofs.close();
```

`(char *)pow` - указатель на массив 
`sizeof(pow)` - размер 