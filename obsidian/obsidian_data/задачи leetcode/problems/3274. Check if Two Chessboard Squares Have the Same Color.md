```python
class Solution(object):
    def checkTwoChessboards(self, coordinate1, coordinate2):
        a1 = coordinate1
        a2 = coordinate2
        x = 0
        a = '12345678'
        b = 'abcdefgh'
        c = 'aceg'
        d = '2468'
        e = '1357'
        if a1[0] in c:
            if a1[1] in d:
                x = x + 1
            else:
                x = x - 1
        else:
            if a1[1] in e:
                x = x + 1
            else:
                x = x - 1
        if a2[0] in c:
            if a2[1] in d:
                x = x + 1
            else:
                x = x - 1
        else:
            if a2[1] in e:
                x = x + 1
            else:
                x = x - 1
        if x == 2 or x == -2:
            print(x)
            return True
        else:
            print(x)
            return False
```
Вам даны две строки, `coordinate1`и `coordinate2`, представляющие собой координаты клетки на `8 x 8`шахматной доске.


Ниже для справки представлена ​​шахматная доска.
![[Pasted image 20241019161744.png]]

Верните `true`, если эти два квадрата имеют одинаковый цвет, и `false`в противном случае.

Координата всегда будет представлять допустимую клетку шахматной доски. Координата всегда будет иметь букву на первом месте (указывающую на ее столбец), а цифру на втором месте (указывающую на ее строку).