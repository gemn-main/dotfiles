```python
class Solution(object):
    def areaOfMaxDiagonal(self, dimensions):
        if len(dimensions) == 1:
            return dimensions[0][0] * dimensions[0][1]
        def diagonal(x, y):
            diag =  ((x ** 2) + (y ** 2)) ** (0.5)
            return diag

        max_ind = 0
        max_diag = 0
        for i in range(len(dimensions)):
            d = diagonal(dimensions[i][0],dimensions[i][1])
            if d > max_diag:
                max_diag = d
                max_ind = i
            if d == max_diag:
                one = dimensions[max_ind][0] * dimensions[max_ind][1]
                two = dimensions[i][0] * dimensions[i][1]
                if one < two:
                    max_ind = i

        return dimensions[max_ind][0] * dimensions[max_ind][1]
```

Вам дан двумерный целочисленный массив с индексом 0dimensions .

Для всех индексов i, представляет собой длину и 0 <= i < dimensions.lengthпредставляет собой ширину прямоугольника .dimensions[i] [0]dimensions [i] [1] i

Верните площадь прямоугольника, имеющего самую длинную диагональ. Если есть несколько прямоугольников с самой длинной диагональю, верните площадь прямоугольника, имеющего максимальную площадь.

 

Пример 1:

Вход: размеры = [9,3],[8,6]
 Выход: 48
 Пояснение:  
Для индекса = 0, длина = 9 и ширина = 3. Длина диагонали = sqrt(9 * 9 + 3 * 3) = sqrt(90) ≈ 9,487. 
Для индекса = 1, длина = 8 и ширина = 6. Длина диагонали = sqrt(8 * 8 + 6 * 6) = sqrt(100) = 10. 
Таким образом, прямоугольник с индексом 1 имеет большую длину диагонали, поэтому мы возвращаем площадь = 8 * 6 = 48.
Пример 2:

Вход: размеры = [3,4],[4,3]
 Выход: 12
 Пояснение: Длина диагонали у обоих одинакова и равна 5, поэтому максимальная площадь = 12.
 