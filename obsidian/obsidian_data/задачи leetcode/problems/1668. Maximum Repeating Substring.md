```python
class Solution(object):
    def maxRepeating(self, sequence, word):
        k = 0
        leni = len(sequence) // len(word)
        for i in range(1, leni+1):
            if (word * i) in sequence:
                k = i
        return k
```
Для строки sequence, строка wordявляется k-повторяющейся , если wordконкатенированное kчисло раз является подстрокой sequence. Максимальное -повторяющееся значениеword для является наибольшим значением, где -повторяется в . Если не является подстрокой , максимальное -повторяющееся значение для равно

Для заданных строк sequenceи wordвернуть максимальное повторяющееся kзначение из word.sequence

 

Пример 1:

Ввод: последовательность = "ababc", слово = "ab"
 Вывод: 2
 Пояснение: "abab" — это подстрока в " abab c".
Пример 2:

Ввод: последовательность = "ababc", слово = "ba"
 Вывод: 1
 Пояснение: "ba" является подстрокой в ​​"a ba bc". "baba" не является подстрокой в ​​"ababc".
Пример 3:

Ввод: последовательность = "ababc", слово = "ac"
 Вывод: 0
 Пояснение: "ac" не является подстрокой в ​​"ababc". 