```python
class Solution(object):
    def countDigits(self, num):
        str_num = str(num)
        o = 0
        for i in range(len(str_num)):
            if num % int(str_num[i]) == 0:
                o += 1
        return o
```
Для заданного целого числа numвернуть количество цифр в numэтом деленииnum .

Целое число valделится, numsесли nums % val == 0.

 

Пример 1:

Ввод: num = 7
 Вывод: 1
 Пояснение: 7 делится само на себя, следовательно, ответ — 1.
Пример 2:

Ввод: num = 121
 Вывод: 2
 Пояснение: 121 делится на 1, но не на 2. Поскольку 1 встречается дважды как цифра, мы возвращаем 2.
Пример 3:

Ввод: num = 1248
 Вывод: 4
 Пояснение: 1248 делится на все свои цифры, поэтому ответ — 4.
 