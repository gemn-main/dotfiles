```python
class Solution(object):
    def longestAlternatingSubarray(self, nums, threshold):
        o = 0
        b = 0
        len_nums = len(nums)
        for i in range(len_nums):
            if b == 0:
                if nums[i] % 2 != 0:
                    continue
            if i != len_nums-1:
                if nums[i] <= threshold:
                    if nums[i] % 2 != nums[i+1] % 2:
                        b += 1
                    else:
                        b += 1
                        if o < b:
                            o = b
                        b = 0
                else:
                    b = 0
            else:
                if nums[i] <= threshold:
                        b += 1
                else:
                    b = 0
            if o < b:
                o = b
        return o
```
Вам дан целочисленный массив с индексом 0nums и целое число threshold.

Найдите длину самого длинного подмассива , numsначинающегося с индекса lи заканчивающегося индексом r (0 <= l <= r < nums.length), который удовлетворяет следующим условиям:

nums[l] % 2 == 0
Для всех индексов iв диапазоне [l, r - 1],nums[i] % 2 != nums[i + 1] % 2
Для всех индексов iв диапазоне [l, r],nums[i] <= threshold
Верните целое число, обозначающее длину самого длинного такого подмассива.

Примечание: Подмассив — это непрерывная непустая последовательность элементов внутри массива.

 

Пример 1:

Вход: nums = [3,2,5,4], threshold = 5
 Выход: 3
 Пояснение: В этом примере мы можем выбрать подмассив, который начинается с l = 1 и заканчивается на r = 3 => [2,5,4]. Этот подмассив удовлетворяет условиям. 
Следовательно, ответом является длина подмассива, 3. Мы можем показать, что 3 — это максимально возможная достижимая длина.
Пример 2:

Вход: nums = [1,2], threshold = 2
 Выход: 1
 Пояснение: В этом примере мы можем выбрать подмассив, который начинается с l = 1 и заканчивается на r = 1 => [2]. 
Он удовлетворяет всем условиям, и мы можем показать, что 1 — это максимально возможная достижимая длина.
Пример 3:

Вход: nums = [2,3,4,5], threshold = 4
 Выход: 3
 Пояснение: В этом примере мы можем выбрать подмассив, который начинается с l = 0 и заканчивается на r = 2 => [2,3,4]. 
Он удовлетворяет всем условиям. 
Следовательно, ответ — длина подмассива, 3. Мы можем показать, что 3 — это максимально возможная достижимая длина.
 