Функция `chr()` в Python используется для преобразования **числовой кодовой точки Unicode** в соответствующий символ. Это обратная функция для `ord()`.


```python
print(chr(65))    # 'A'
print(chr(97))    # 'a'
print(chr(49))    # '1'
```

[[ord]] 