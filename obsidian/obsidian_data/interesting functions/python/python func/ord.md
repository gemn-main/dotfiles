Функция `ord()` в Python используется для получения **кодовой точки Unicode** (численного представления) символа. Это обратная функция для `chr()`.

```python
print(ord('A'))  # 65
print(ord('a'))  # 97
print(ord('1'))  # 49
```

[[chr]] 