```python      
data = wineq[:,:-1]

target = wineq[:, 1]
print(target, target.shape)

...

tensor([0.2700, 0.3000, 0.2800,  ..., 0.2400, 0.2900, 0.2100]) torch.Size([4898])
```
`data` - хранит все ВД кроме последнего столбика 
`target` - только последний, он и должен выть на выходе (в идеале )