```python
import torch
import numpy as np

bikesNP = np.loadtxt(
    "/home/user/data/bike_sharing_data/hour.csv",
    dtype = np.float32,
    delimiter=',',
    skiprows = 1,
    converters = {1: lambda x: float(x[8:10])})
    
bikes = torch.from_numpy(bikesNP)
n_full_days = bikes.shape[0] // 24  
full_bikes = bikes[:n_full_days * 24]
print(bikes.shape)
daily_bikes = full_bikes.view(-1, 24, bikes.shape[1])
print(daily_bikes.shape, daily_bikes.stride())

daily_bikes = daily_bikes.transpose(1, 2)
print(daily_bikes.shape, daily_bikes.stride())

...
torch.Size([17379, 17])                                                   
torch.Size([724, 24, 17]) (408, 17, 1)                                    
torch.Size([724, 17, 24]) (408, 1, 17)                                    
```

сначала загружаем данные:
```python
bikesNP = np.loadtxt(
    "/home/user/data/bike_sharing_data/hour.csv",
    dtype = np.float32,
    delimiter=',',
    skiprows = 1,
    converters = {1: lambda x: float(x[8:10])})
```

находим сколько дней в наборе(изначально был представлен просто списком по часам, где заканчивался 24 начинался 1)
```python
n_full_days = bikes.shape[0] // 24 
```

берем только столько строк чтобы они делились без остатка на 24
```python
full_bikes = bikes[:n_full_days * 24]
```

переформируем массив 
```python
daily_bikes = full_bikes.view(-1, 24, bikes.shape[1])
daily_bikes = daily_bikes.transpose(1, 2)
```
