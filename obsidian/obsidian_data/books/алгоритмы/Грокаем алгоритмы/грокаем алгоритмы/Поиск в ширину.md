```python
from collections import deque

graph = {}
graph['you'] = ["alice", 'bob', 'claire']
graph['alice'] = ['lolik', 'makcim']
graph['bob'] = ['mam boba']
graph['claire'] = ['brown', "john"]
graph['lolik'] = []
graph['makcim'] = []
graph['mam boba'] = []
graph['brown'] = []
graph['john'] = []

def person_is_seller(name):
    return name[-1] == 'k'

def search_name(name):
    search_queue = deque()`
    search_queue += graph[name]
    searched = []
    while search_queue:
        person = search_queue.popleft()
        if not person in searched:
            if person_is_seller(person):

                print (person + ' is a mango seller!')
                return 
            else:
                search_queue += graph[person]
                searched.append(person)
    return print('no')

search_name('you')
```
Вычисляет кратчайший путь в невзвешенном графе  


*deque()* - создает очередь 
*popleft()* - из очереди извлекается первый человек 