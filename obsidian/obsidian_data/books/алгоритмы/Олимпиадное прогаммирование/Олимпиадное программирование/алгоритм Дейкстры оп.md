- требуется чтобы в графе не было ребер с отрицательным весом 
```cpp
for (int i = 1; i <= n; i++)
{
	distancee[i] = 1e9;
}
distancee[1] = 0;
q.push({0,1});
while (!q.empty()){
	int a = q.top().second; q.pop();
	if (processed[a]) continue;
	precessed[a] = true;
	for (auto u : adj[a]){
		int b = u.first, w = u.second;
		if (distancee[a] + w < distancee[b]){
			distancee[b] = distancee[a]+w;
			q.push({-distancee[b], b});
		}
	}
}
```
- **Вес рёбер:**
    
    - `A → B = 3`
    - `A → C = 2`
    - `B → D = 1`
    - `B → E = 4`
    - `C → B = 1`
    - `C → E = 5`
    - `E → F = 3`
    - `F → D = 2`

## **🔹 Пошаговая работа алгоритма Дейкстры:**

### **📌 Шаг 1: Инициализация**

- Все расстояния `∞`, кроме `A`.
- `distance = {A:0, B:∞, C:∞, D:∞, E:∞, F:∞}`
- Очередь (`min-heap`): `[(0, A)]`
    

### **📌 Шаг 2: Обрабатываем `A`**

- Достаём `(0, A)`.
    
- Релаксация рёбер из `A`:
    
    - `A → B = 3` → `distance[B] = min(∞, 0+3) = 3`
        
    - `A → C = 2` → `distance[C] = min(∞, 0+2) = 2`
        
- Очередь: `[(2, C), (3, B)]`
    

**Состояние:**  
`distance = {A:0, B:3, C:2, D:∞, E:∞, F:∞}`

### **📌 Шаг 3: Обрабатываем `C` (минимальное `distance = 2`)**

- Достаём `(2, C)`.
    
- Релаксация рёбер из `C`:
    
    - `C → B = 1` → `distance[B] = min(3, 2+1) = 3` (не изменилось)
        
    - `C → E = 5` → `distance[E] = min(∞, 2+5) = 7`
        
- Очередь: `[(3, B), (7, E)]`
    

**Состояние:**  
`distance = {A:0, B:3, C:2, D:∞, E:7, F:∞}`

### **📌 Шаг 4: Обрабатываем `B` (`distance = 3`)**

- Достаём `(3, B)`.
    
- Релаксация рёбер из `B`:
    
    - `B → D = 1` → `distance[D] = min(∞, 3+1) = 4`
        
    - `B → E = 4` → `distance[E] = min(7, 3+4) = 7` (не изменилось)
        
- Очередь: `[(4, D), (7, E)]`
    

**Состояние:**  
`distance = {A:0, B:3, C:2, D:4, E:7, F:∞}`

### **📌 Шаг 5: Обрабатываем `D` (`distance = 4`)**

- Достаём `(4, D)`.
    
- У `D` нет исходящих рёбер → ничего не делаем.
    
- Очередь: `[(7, E)]`
    

**Состояние:**  
`distance = {A:0, B:3, C:2, D:4, E:7, F:∞}`

### **📌 Шаг 6: Обрабатываем `E` (`distance = 7`)**

- Достаём `(7, E)`.
    
- Релаксация рёбер из `E`:
    
    - `E → F = 3` → `distance[F] = min(∞, 7+3) = 10`
        
- Очередь: `[(10, F)]`
    

**Состояние:**  
`distance = {A:0, B:3, C:2, D:4, E:7, F:10}`

### **📌 Шаг 7: Обрабатываем `F` (`distance = 10`)**

- Достаём `(10, F)`.
    
- Релаксация рёбер из `F`:
    
    - `F → D = 2` → `distance[D] = min(4, 10+2) = 4` (не изменилось)
        
- Очередь: `[]` (пустая)
    

**Финальное состояние:**  
`distance = {A:0, B:3, C:2, D:4, E:7, F:10}`

```cpp
        (A) ——3→ (B) ——1→ (D)
         |   1   ↗ |       ↑
        2|    /   |4      |2
         ↓ /      ↓       |
        (C) ——5→ (E) ←3— (F)
```
