```cpp
int main()
{
    int array[] = {1, 5, 1, 0, -5, 1, -52, -5, 10, -20, 21};
    int best = 0, sum = 0;
    int n = sizeof(array) / sizeof(array[0]);
    for (int k = 0; k < n; k++)
    {
        sum = max(array[k], sum + array[k]);
        best = max(best, sum);
    }
    cout << best << endl;
    return 0;
}
...
21
```